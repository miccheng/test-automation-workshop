{
  "name": "JavaScript Dev",
  "image": "ghcr.io/tdd-workshops/devcontainer-js",
  "forwardPorts": [
    9323,
    3000,
    5173
  ],
  "postCreateCommand": "pushd ./todo-frontend && npm install && popd && pushd ./todo-backend/todo-app-js && npm install && popd && pushd ./e2e/js && npm install && popd",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-playwright.playwright",
        "MS-vsliveshare.vsliveshare",
        "humao.rest-client",
        "Vue.volar",
        "oderwat.indent-rainbow",
        "vitest.explorer"
      ]
    }
  }
}